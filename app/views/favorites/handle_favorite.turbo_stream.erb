<% if @favorite.destroyed? %>
  <%= turbo_stream.remove "favorite_#{@favorite.id}" %>
<% else %>
  <%= turbo_stream.append "favorites", @favorite %>
<% end %>

<%= render_turbo_stream_flash_messages %>